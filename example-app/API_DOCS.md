# My API

API generated from Glossify annotations

_Generated by Glossify_

---

## UserController

### `GET` /users

Get all users from the database

**Responses:**

- **200**:
```json
[{ "id": 1, "name": "John" }, { "id": 2, "name": "Jane" }]
```

---

### `GET` /users/:id

Get a specific user by ID

**Parameters:**

| Name | Location | Description |
|------|----------|-------------|
| `id` | path | The user's unique identifier |

**Responses:**

- **200**:
```json
{ "id": 1, "name": "John", "email": "john@example.com" }
```

---

### `POST` /users

Create a new user

**Request Body:**

```json
{ "name": "string", "email": "string", "password": "string" }
```

**Responses:**

- **201**:
```json
{ "id": 3, "name": "New User", "message": "User created successfully" }
```

---

### `PUT` /users/:id

Update an existing user

**Parameters:**

| Name | Location | Description |
|------|----------|-------------|
| `id` | path | The user's unique identifier |

**Request Body:**

```json
{ "name": "string", "email": "string" }
```

**Responses:**

- **200**:
```json
{ "message": "User updated successfully" }
```

---

### `DELETE` /users/:id

Delete a user

**Parameters:**

| Name | Location | Description |
|------|----------|-------------|
| `id` | path | The user's unique identifier |

**Responses:**

- **204**:
```json
null
```

---

## ProductController

### `GET` /products

List all products

**Parameters:**

| Name | Location | Description |
|------|----------|-------------|
| `limit` | path | Number of items to return |
| `offset` | path | Pagination offset |
| `limit` | query | Number of items to return |
| `offset` | query | Pagination offset |

**Responses:**

- **200**:
```json
{ "products": [], "total": 0 }
```

---

### `GET` /products/:id

Get product details

**Responses:**

- **200**:
```json
{ "id": 1, "name": "Widget", "price": 9.99 }
```

---

